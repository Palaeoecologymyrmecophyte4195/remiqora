## What does this change?

<!-- A short description and the motivation. Link the issue it closes, if any: "Closes #123". -->

## How was it tested?

<!-- OS / hardware and what you ran. Screenshots for UI changes. -->

## Checklist

- [ ] `npm run build` passes in `frontend/` (CI runs the same)
- [ ] New user-facing strings are added to both `frontend/src/locales/en.ts` and `ru.ts`
- [ ] No model weights, `.env` files or logs are included
